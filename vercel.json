{
    "builds": [
        {
            "use": "@vercel/node",
            "src": ".",
            "config": {
                "command": "npx prisma generate && npm run build"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "pages/index.tsx"
        }
    ]
}